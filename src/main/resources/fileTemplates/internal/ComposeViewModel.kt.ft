package $PACKAGE_NAME

import org.koin.core.annotation.Factory
import ${FEATURES_PACKAGE_NAME}.base.BaseViewModel
import ${FEATURES_PACKAGE_NAME}.${FEATURE_NAME}.${NAME}Event
import ${FEATURES_PACKAGE_NAME}.${FEATURE_NAME}.${NAME}UiState
import ${FEATURES_PACKAGE_NAME}.${FEATURE_NAME}.${NAME}Contract


@Factory
class ${NAME}ViewModel(

) : BaseViewModel<${NAME}UiState, ${NAME}Event>(${NAME}UiState()), ${NAME}Contract {

}
