package $PACKAGE_NAME

import androidx.compose.runtime.Stable
import org.koin.android.annotation.KoinViewModel
import ${FEATURES_PACKAGE_NAME}.base.BaseViewModel


@Stable
@KoinViewModel
class ${NAME}ViewModel(

) : BaseViewModel<${NAME}UiState, ${NAME}Effect>(${NAME}UiState()), ${NAME}Actions {

}
